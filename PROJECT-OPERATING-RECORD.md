# سجل المشروع — Baheth Masr Crawler

## الغرض

هذا الملف هو المرجع التشغيلي والمعماري للمشروع. يوضح حدود كل مكوّن، ومسؤولية كل جدول، وترتيب انتقال البيانات. يجب تحديثه عند تغيير سلوك أي Workflow أو جدول حتى يستطيع مطور أو نظام AI متابعة المشروع دون تخمين.

## القاعدة الأساسية

> العنكبوت يكتشف ويسجل الروابط فقط. المراقب يعيد فحص المواقع والصفحات ويضيف الجديد فقط. زاحف الفهرسة يقرأ الصفحات المسجلة ويفهرسها فقط.

لا يجوز لزاحف الفهرسة أن يضيف مواقع أو صفحات جديدة أثناء الفهرسة. الاكتشاف والفهرسة مرحلتان منفصلتان.

## قواعد البيانات

| الاسم التشغيلي | الاستخدام | الجداول الأساسية |
|---|---|---|
| قاعدة الزحف 2 | مصدر التحكم، الاكتشاف، الطوابير، نتائج الزيارات والمراجعة | `sites`, `site_pages`, `crawl_site_queue`, `crawl_targets`, `crawl_discoveries`, `crawl_results`, `crawl_review_items`, `crawl_quarantine` |
| قاعدة الإنتاج 1 | الصفحات المقبولة التي يقرأ منها محرك البحث | `sites`, `site_pages`, `site_search_fts` وفهارس FTS الداخلية |

قاعدة الزحف ليست مصدر نتائج البحث النهائي. لا تُرسل إلى الإنتاج إلا النتائج التي اجتازت التنظيف والتحقق والاعتماد.

## المكوّنات والملكية

### Discovery Spider — العنكبوت

الملف: `tools/discovery-spider.mjs`  
الجدولة: `.github/workflows/discovery-spider.yml`، مرة كل 24 ساعة.

مسؤوليته هي اكتشاف المواقع والصفحات فقط. يقرأ المواقع التي لم تنتهِ حالة اكتشافها، يزور الصفحة الرئيسية والصفحات الداخلية ضمن حد الموقع، يسجل الصفحة الرئيسية باعتبارها صفحة، ويضيف الصفحات الداخلية إلى `site_pages` بحالة `pending`. الروابط الخارجية تُقبل فقط إذا كانت HTTP/HTTPS وامتداد نطاقها من القائمة المسموحة، ويُستبعد مسار الملف والروابط غير الصالحة. عند العثور على نطاق جديد يسجل موقعًا جديدًا ويضيف الرابط الأول له كصفحة.

العنكبوت لا يكتب إلى قاعدة الإنتاج ولا يعتمد المحتوى للبحث.

### Indexed Sites Monitor — المراقب

الملف: `tools/indexed-sites-monitor.mjs`  
الجدولة: `.github/workflows/indexed-sites-monitor.yml`، مرة كل ساعتين.

يفحص المواقع النشطة، ويحاول قراءة `sitemap.xml` والصفحة الرئيسية. يضيف الروابط الجديدة إلى `site_pages` فقط، ويحدث `sites.last_checked_at`. لا يفهرس المحتوى ولا يكتب إلى قاعدة الإنتاج. تاريخ `last_checked_at` هو مرجع الجولة الأخيرة، بينما `last_discovered_at` خاص بجولة العنكبوت.

### Page Indexer — زاحف الفهرسة

الجدولة: `.github/workflows/page-indexer.yml`، كل 30 دقيقة.

يقرأ `site_pages` والصفحات غير المفهرسة فقط. يعالج 50 صفحة في الدفعة وبحد أقصى 500 صفحة في التشغيل. يستخدم HTTP أولًا، ثم Chromium عند فشل HTTP أو انخفاض جودة المحتوى، مع حد أقصى 7 متصفحات متزامنة. ينظف HTML، يتحقق من النص، يعزل الفاشل، ويرسل المقبول إلى قاعدة الإنتاج 1 وفهرس البحث.

هذا المسار لا يشغل اكتشاف الروابط لأن `CRAWLER_DISCOVERY_MODE=0`.

### Queue Workflow — التشغيل اليدوي القديم

الملف: `.github/workflows/crawl-site-queue.yml`.

لم تعد له جدولة تلقائية حتى لا يختلط اكتشاف المواقع بالفهرسة الدورية. يظل متاحًا يدويًا لإدارة `crawl_site_queue` أثناء الانتقال، لكن التشغيل الدوري المعتمد هو Discovery Spider للمواقع وPage Indexer للصفحات.

## ملكية الجداول

| المكوّن | `sites` | `site_pages` | الإنتاج |
|---|---|---|---|
| العنكبوت | إنشاء وتحديد حالة اكتشاف المواقع | إضافة الصفحات المكتشفة | لا يكتب |
| المراقب | تحديث وقت الفحص | إضافة الصفحات الجديدة وتحديث وقت الفحص | لا يكتب |
| زاحف الفهرسة | قراءة مرجعية | قراءة الصفحات وتحديث حالة الفهرسة عند التطبيق الكامل | يرسل المقبول فقط |

حقول المراقبة الخفيفة في migration 007 هي: `sites.discovery_status`, `sites.last_checked_at`, `sites.last_discovered_at`, `site_pages.discovered_at`, `site_pages.last_checked_at`, و`site_pages.indexed_at`.

## مسار البيانات

```text
Discovery Spider كل 24 ساعة
    ↓
المواقع والصفحات المكتشفة في قاعدة الزحف 2
    ↓
Indexed Sites Monitor كل ساعتين
    ↓
إضافة الصفحات الجديدة فقط إلى site_pages
    ↓
Page Indexer كل 30 دقيقة
    ↓
تنظيف HTML والتحقق والمراجعة
    ↓
قاعدة الإنتاج 1 + site_search_fts
```

## قواعد الروابط

يُقبل الرابط إذا كان HTTP أو HTTPS، وكان نطاقه من الامتدادات المشهورة المحددة في العنكبوت: `.eg`, `.com`, `.net`, `.org`, `.edu`, `.gov`, `.ai`, `.jp`. يجب توسيع القائمة فقط عند الحاجة الفعلية.

تُستبعد الملفات المعروفة مثل PDF والصور والصوت والفيديو والملفات المضغوطة والبرامج، إضافة إلى `mailto:`, `tel:` و`javascript:`، وروابط البحث والفلاتر عند تطبيق فلترها في الاستخراج. يجب استخدام `canonicalize` لمنع معاملات التتبع والتكرار.

## حدود التشغيل والتوفير

لا تضف فهارس جديدة دون قياس استعلام بطيء. لا تحفظ HTML الخام في قاعدة الإنتاج. لا تشغل AI على كل صفحة؛ إذا أضيفت طبقة AI مستقبلًا فيجب تشغيلها فقط للصفحات المقبولة ذات الوصف أو التصنيف الناقص، وربط النتيجة بـ`content_hash`.

يجب تنظيف نتائج `crawl_results` و`crawl_review_items` القديمة وفق سياسة احتفاظ لاحقة، مع الإبقاء على أخطاء قابلة لإعادة المحاولة. لا تحذف جداول FTS الداخلية يدويًا.

## التحقق قبل الرفع

شغّل:

```bash
node --check crawler/src/crawl.mjs
node --check tools/discovery-spider.mjs
node --check tools/indexed-sites-monitor.mjs
npm test
 git diff --check
```

عند تغيير مخطط، يجب تشغيل `node tools/apply-turso-schema.mjs` في بيئة GitHub ثم فحص القاعدتين قراءة فقط. لا تستخدم Workflow reset في التشغيل العادي.

## ملاحظات انتقالية

المسار القديم كان يجمع اكتشاف الروابط والفهرسة في Workflow واحد. تم فصل الجدولة الجديدة إلى ثلاثة Workflows. يجب عدم إعادة تفعيل جدولة `Crawl One Queued Site` إلا بعد تحويله بالكامل إلى اكتشاف فقط أو إلى إدارة طابور يدوية صريحة.
