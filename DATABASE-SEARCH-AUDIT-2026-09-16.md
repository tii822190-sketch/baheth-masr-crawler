# تقرير تقييم البحث وقاعدة البيانات

**التاريخ:** 2026-09-16 18:53 بتوقيت القاهرة  
**نطاق الفحص:** اختبار البحث العام، مراجعة مخططات الجداول والفهارس، ومراجعة عدادات الإنتاج والزحف.  
**التغييرات المنفذة:** لا توجد. هذا التقرير قراءة فقط.

## النتيجة التنفيذية

قاعدة البيانات الحالية مناسبة لتوسيع المشروع تدريجيًا، ولا تحتاج إلى إعادة تصميم أو إضافة جداول كبيرة في الوقت الحالي. التصميم يفصل بين قاعدة الزحف المرحلية وقاعدة الإنتاج، ويستخدم فهرس FTS5 للبحث النصي. هذا الفصل قرار صحيح لأنه يمنع بيانات المحاولات والأخطاء من تضخيم قاعدة الإنتاج.

مع ذلك، لا يُنصح بتشغيلها بلا سياسة احتفاظ للبيانات المرحلية. قاعدة الزحف تحتوي على 967 نتيجة و923 عنصر مراجعة، بينما قاعدة الإنتاج تحتوي على 493 صفحة و590 سجلًا في فهرس البحث. إذا استمر الاحتفاظ بكل نتائج الزحف والمحاولات القديمة، فستزداد المساحة والكتابات بلا فائدة بحثية مباشرة.

**الحكم:** لا تعدّل مخطط الإنتاج الآن. نفّذ لاحقًا تحسينات تشغيلية صغيرة تقلل الكتابة والتكرار، مع الحفاظ على أسماء الجداول والأعمدة الحالية.

## اختبار البحث

| الاستعلام | النتيجة المرصودة | التقييم |
|---|---:|---|
| التعليم | 27 نتيجة | النتائج الأولى كانت من بوابة محافظة القاهرة، وعناوينها وأوصافها مرتبطة مباشرة بالتعليم. ظهرت صفحات متقاربة من الموقع نفسه، وهذا منطقي لكنه يحتاج تنويعًا عند زيادة عدد المواقع. |
| خدمات حكومية إلكترونية | 3 نتائج | النتائج كانت مرتبطة بالخدمات الحكومية الرقمية وبوابة محافظة القاهرة. النتيجة الثالثة أقل تطابقًا من الأوليين، لكنها ما تزال ضمن السياق الحكومي. |
| أسعار الذهب | لا توجد نتائج | النتيجة صحيحة بالنسبة للبيانات المفهرسة حاليًا؛ لا توجد ضوضاء أو نتائج بعيدة لمجرد وجود كلمات عامة. |

البحث يعرض نصوصًا نظيفة من HTML، ويعرض العنوان والرابط والوصف. الاختبارات تشير إلى أن FTS يعمل بصورة صحيحة في المرحلة الحالية. لا يمكن اعتبار ذلك قياسًا إحصائيًا شاملًا للدقة قبل فهرسة عدد أكبر من المواقع وتكوين مجموعة استعلامات مرجعية.

## حالة قاعدة الإنتاج

| العنصر | العدد المرصود | القراءة الفنية |
|---|---:|---|
| `sites` | 4 | عدد قليل حاليًا، وسيزداد مع إضافة المواقع المصرية. |
| `site_pages` | 493 | صفحات الإنتاج الفعلية. |
| `site_search_fts` | 590 | سجلات البحث، وتشمل سجلات المواقع والصفحات أو السجلات المرتبطة بها. يجب مراقبة سبب زيادة العدد عن الصفحات عند التوسع. |
| FTS shadow tables | 506 إلى 596 حسب الجدول | جداول داخلية لازمة لـ FTS5، ولا ينبغي حذفها يدويًا. |

وجود جداول FTS الداخلية مثل `site_search_fts_data` و`site_search_fts_docsize` و`site_search_fts_idx` طبيعي. حذفها أو التعامل معها كجداول عادية قد يكسر الفهرس. كما أن زيادة عدد سجلات FTS عن `site_pages` ليست دليلًا وحدها على خطأ، لأن الفهرس يحتوي أيضًا على سجلات المواقع وبعض السجلات السابقة أو الأنواع الأخرى؛ لكنها تستحق فحصًا دوريًا عند تجاوز الفارق نسبة كبيرة.

## حالة قاعدة الزحف

قاعدة الزحف تحتوي على 967 سجلًا في `crawl_results` و923 عنصرًا في `crawl_review_items` و942 رابطًا مكتشفًا و976 هدف زحف. هذه الجداول مفيدة أثناء التشغيل والمراجعة، لكنها ليست مصدر البحث النهائي. الاحتفاظ بها بلا حدود سيستهلك مساحة وسيزيد زمن المزامنة والنسخ المرحلي.

هناك 12 نتيجة في `crawl_quarantine`. ينبغي الاحتفاظ بها مؤقتًا لإعادة المحاولة، ثم حذف أو أرشفة النتائج التي تجاوزت عددًا محددًا من المحاولات. لا توجد حاجة لزيادة أعمدة أو جداول لهذا الغرض؛ يمكن استخدام الحقول الحالية مثل `retry_status` و`attempts` و`created_at`.

## ملاءمة المخطط للتوسع

المخطط الحالي مناسب من ناحية فصل المسؤوليات:

- `sites` و`site_pages` يحفظان الكيان الإنتاجي المطلوب للبحث.
- `site_search_fts` يوفر البحث النصي السريع.
- `crawl_targets` و`crawl_discoveries` يديران الطابور والاكتشاف.
- `crawl_results` و`crawl_review_items` يفصلان نتيجة الجلب عن قرار الاعتماد.
- `crawl_quarantine` يحتفظ بالأخطاء القابلة لإعادة المحاولة.

هذا التصميم مناسب للتوسع من مئات الصفحات إلى عشرات الآلاف، بشرط التحكم في دورة حياة بيانات الزحف. عند الوصول إلى مئات الآلاف أو ملايين الصفحات، ستصبح الحاجة أكبر إلى تقسيم التشغيلات، وتقليل حجم النصوص المرحلية، ومراقبة زمن تحديث FTS، لكن لا توجد إشارة حاليًا تستدعي إعادة بناء المخطط.

## التعديلات الموصى بها لتوفير المساحة والكتابات

### 1. عدم تغيير مخطط الإنتاج الآن

لا تضف نسخًا إضافية من النص الكامل في جداول جديدة. حقل `content` و`search_text` موجودان بالفعل في `site_pages`، وفهرس FTS يحتاج نسخة مفهرسة للبحث. إضافة جدول محتوى آخر ستضاعف المساحة بلا فائدة واضحة.

### 2. منع تحديث الصفحة إذا لم يتغير `content_hash`

مسار المزامنة الحالي ينفذ إدخالًا ثم تحديثًا لكل نتيجة، ويعيد حذف وإدخال سجل FTS للصفحات. الأفضل مستقبلاً مقارنة `content_hash` أولًا. إذا لم يتغير المحتوى، فلا داعي لتحديث النص أو إعادة بناء سجل FTS. هذا سيقلل الكتابات خصوصًا عند إعادة الزحف الدوري.

### 3. تنظيف قاعدة الزحف بسياسة احتفاظ

بعد نجاح المزامنة، يمكن الاحتفاظ بآخر تشغيلين أو ثلاثة لكل موقع، مع حذف النتائج الأقدم من فترة محددة. يجب الإبقاء على نتائج `quarantine` التي ما زالت قابلة لإعادة المحاولة، وحذف النتائج النهائية التي تجاوزت حد المحاولات. هذا الإجراء يوفر مساحة دون المساس بجداول الإنتاج.

### 4. عدم حذف جداول FTS الداخلية

لا تحذف `site_search_fts_data` أو `site_search_fts_idx` أو الجداول shadow الأخرى. التوفير الصحيح يكون عبر إزالة السجلات المكررة من جدول FTS الرئيسي عند الحاجة، ثم استخدام آليات FTS الرسمية مثل إعادة البناء في نافذة صيانة، وليس عبر حذف الجداول الداخلية مباشرة.

### 5. إبقاء الفهارس الحالية

الفهارس الموجودة في قاعدة الزحف تغطي الحالات المهمة: حالة الهدف، أولوية الهدف، زمن التشغيل، حالة الاكتشاف، حالة المراجعة، وحالة إعادة المحاولة. إضافة فهارس كثيرة ستزيد تكلفة الكتابة. لا أوصي بإضافة فهرس جديد قبل ظهور استعلام بطيء مثبت بقياس فعلي.

### 6. معالجة الموقع العالق تشغيليًا

حالة `misrquran.gov.eg` كانت `processing` بعد عدة محاولات رغم عدم وجود تشغيل نشط. هذه ليست مشكلة مخطط قاعدة البيانات. ينبغي أن يعيد نظام الاسترداد الحالات القديمة من `processing` إلى `pending` بعد مهلة، أو عند انتهاء تشغيل GitHub دون خطوة إكمال. يمكن تنفيذ ذلك بتحديث صف الطابور الحالي، دون إضافة جدول جديد.

## الخلاصة

قاعدة البيانات الحالية **مناسبة للمشروع والتوسع التدريجي**، وفهرس البحث يعمل بصورة جيدة في الاختبارات الحالية. لا توجد حاجة الآن إلى تعديل الأعمدة أو حذف جداول FTS أو إضافة طبقة تخزين جديدة.

أفضل تحسين اقتصادي هو التحكم في بيانات قاعدة الزحف، ثم منع إعادة كتابة الصفحات والفهرس عندما لا يتغير `content_hash`. هذان الإجراآن سيقللان استهلاك المساحة ومعدلات القراءة والكتابة أكثر من إعادة تصميم المخطط. يجب تأجيل أي تغيير بنيوي إلى أن يتجاوز حجم الإنتاج عشرات الآلاف من الصفحات أو تظهر قياسات فعلية لمشكلة أداء.

## References

[1]: https://www.sqlite.org/fts5.html "SQLite FTS5 Extension Documentation"

[2]: https://docs.turso.tech/sdk/http/reference "Turso SQL over HTTP Reference"

[3]: https://www.sqlite.org/foreignkeys.html "SQLite Foreign Key Support"
