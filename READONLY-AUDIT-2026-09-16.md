# تقرير فحص القراءة فقط — Baheth Masr Crawler

**التاريخ:** 2026-09-16

**نطاق الفحص:** قراءة ملفات المستودع، حالة Git، تعريفات الجداول، مهام GitHub Actions، الإعدادات، ثم تنفيذ طلبات `GET /health` و`SELECT` فقط على قواعد Turso. لم تُنفّذ أي عملية كتابة أو تعديل أو حذف في المستودع أو قواعد البيانات.

## الخلاصة التنفيذية

المستودع هو نظام زحف مرحلي يعتمد على SQLite محليًا أثناء تشغيل GitHub Actions، ثم يرفع نسخة staging إلى Turso. توجد قاعدة بيانات للتحكم في المواقع والأهداف والاكتشافات، وقاعدة نتائج منفصلة append-only. قاعدة Turso الأولى متاحة للقراءة وتحتوي على موقعين وثلاث صفحات مفهرسة. قاعدة Alzahf استجابت لفحص الصحة بـ`HTTP 200`، لكنها رفضت طلب SQL بـ`HTTP 401`؛ لذلك لا يمكن تأكيد جداولها أو محتواها من هذا الفحص.

يوجد تعارض مهم في الإعدادات: اسم موصل قاعدة الزاحف يشير إلى قاعدة `baheth-masr-krmm`، بينما ملف Workflow يضع `TURSO_CRAWLER_DATABASE_URL` على قاعدة `alzahf-karamkkr`. هذا يحتاج قرارًا قبل أي إصلاح أو تنظيف، لأن تغيير الموصل أو Workflow قد يوجّه الزاحف إلى قاعدة مختلفة.

## خريطة المستودع

| الجزء | الملفات الرئيسية | الدور |
|---|---|---|
| التشغيل | `crawler/src/cli.mjs`, `crawler/src/crawl.mjs` | تهيئة قاعدة التشغيل، بدء التشغيل، اختيار الأهداف، الجلب، الحفظ والتوزيع |
| قاعدة التحكم | `crawler/src/db.mjs`, `migrations/001_crawler_staging.sql` | `links.sqlite` للمواقع والصفحات والأهداف والاكتشافات |
| قاعدة النتائج | `migrations/002_crawler_results.sql` إلى `005_classification.sql` | `results.sqlite` لسجل الزيارات، المراجعة، العزل، التحقق والتصنيف |
| طابور المواقع | `migrations/006_crawl_site_queue.sql`, `tools/crawl-queue.mjs` | طابور مستقل لزحف موقع واحد مع حالات ومحاولات |
| الاستخراج | `crawler/src/extract.mjs` | إزالة عناصر غير المحتوى، استخراج النص والوصف والروابط، hash، اللغة والجودة |
| التصنيف | `crawler/src/classify.mjs`, `taxonomy/search-taxonomy.json` | مرشح الفئة والتصنيف الفرعي ودرجة الثقة دون اعتماد تلقائي |
| الاكتشاف | `crawler/src/discovery.mjs` | إدخال روابط يدوية ونتائج DuckDuckGo ضمن حدود محددة |
| المزامنة | `tools/sync-turso-staging.mjs`, `tools/sync-approved-production.mjs` | رفع staging ومزامنة النتائج المعتمدة إلى الإنتاج |
| التحقق | `tools/validate-staging.mjs` | فحص نجاح الجلب، HTML، جودة النص، التكرار والتصنيف |
| النسخ الاحتياطي والفحص | `tools/backup-production.mjs`, `tools/check-*.mjs`, `tools/stress-*.mjs` | قراءة/نسخ احتياطي/اختبارات ضغط لقواعد البيانات |
| التكامل القديم/المحيطي | `integrations/google-apps-script`, `integrations/google-sheets-export.mjs` | تكاملات تصدير وتحكم خارج المسار الأساسي |
| Worker | `worker/src/index.js`, `worker/wrangler.toml` | طبقة تحكم تطلق Workflow GitHub، وليست محرك الزحف نفسه |

## مسارات التشغيل الحالية

يوجد تسعة Workflows:

1. `crawler.yml`: تشغيل الزاحف الأساسي، الدفعات، checkpoint، التحقق والمزامنة.
2. `crawl-site-queue.yml`: معالجة طابور زحف موقع واحد.
3. `sync-approved-production.yml`: مزامنة النتائج المعتمدة إلى الإنتاج.
4. `backup-production.yml`: نسخة احتياطية للإنتاج.
5. `check-both-databases.yml` و`check-production.yml`: فحوصات قراءة.
6. `stress-production-read.yml`: اختبار ضغط قراءة.
7. `reset-databases.yml`: عملية تدميرية محتملة، يجب إبقاؤها منفصلة وحمايتها.
8. `deploy-worker.yml`: نشر Worker التحكم.

الإعدادات المستخدمة تشمل التزامن، المهلة، التأخير، نمط الجلب، عدد الصفحات، حجم الدفعة، عدد التمريرات، الرابط اليدوي، استعلامات البحث، واستكمال الاكتشافات. ملف Workflow يضبط `CRAWLER_FETCH_MODE=hybrid` و`CRAWLER_CONCURRENCY=10` و`CRAWLER_MAX_PAGES=500`.

## قاعدة Turso الأولى — المتاحة

**العنوان:** قاعدة `baheth-masr-krmm` عبر موصل الزاحف.

| القياس | النتيجة |
|---|---:|
| `/health` | HTTP 200 |
| الجداول/الكائنات المكتشفة | 9 جداول، 10 فهارس، 6 مشغلات |
| `sites` | 2 صف |
| `site_pages` | 3 صفوف |
| `site_search_fts` | 5 صفوف |

الجداول التشغيلية الظاهرة في قاعدة الزاحف هي `sites` و`site_pages` و`site_search_fts`، إضافة إلى جداول FTS الداخلية. توجد مشغلات لمزامنة إدخالات وتحديثات وحذف `sites` و`site_pages` مع فهرس البحث النصي.

## قاعدة Turso الثانية — Alzahf

**فحص الصحة:** نجح بـ`HTTP 200`.

**فحص SQL للقراءة:** فشل بـ`HTTP 401 Unauthorized` عند طلب `POST /v2/pipeline`. لذلك لم يتم تأكيد المخطط أو عدد السجلات أو صلاحيات القراءة. لم تتم محاولة أي كتابة أو تدوير رمز أو تعديل موصل.

## ملاحظات تقنية مهمة

- التشغيل يعتمد على `better-sqlite3` و`cheerio`، ويستخدم SQLite محليًا بدل الاتصال المباشر بقاعدة Turso أثناء الزحف.
- اختبار `npm test` في بيئة الفحص لم يبدأ بسبب عدم وجود binding مبني لـ`better-sqlite3` على Node 22. إعادة البناء فشلت لأن أداة البناء `cc` غير موجودة في بيئة الفحص. هذا عائق بيئي، وليس دليلًا وحده على خطأ في اختبارات المشروع.
- إعداد `CRAWLER_FETCH_MODE=hybrid` موجود في Workflow، لكن يلزم في مرحلة لاحقة التأكد من أن التطبيق نفسه يطبق fallback الهجين كما هو متوقع.
- `CRAWLER_MAX_PAGES` يُستخدم في حلقات Workflow، بينما يلزم التأكد من أن حد الصفحات مطبق كذلك داخل مسار الزاحف نفسه حتى لا يعتمد الأمان على طبقة واحدة.
- قاعدة النتائج append-only، والعزل (`quarantine`) والمراجعة (`review_queue`) مصممان لمنع إدخال صفحات فاشلة أو منخفضة الجودة مباشرة إلى الإنتاج.
- وجود Workflow لإعادة ضبط القواعد لا يعني أنه غير لازم، لكنه إجراء عالي الخطورة ويجب ألا يدخل في التشغيل الاعتيادي أو التنظيف التلقائي.
- تكامل Google Sheets يبدو للتصدير/الإدارة وليس قاعدة تشغيل؛ لا ينبغي اعتباره جزءًا من المسار الأساسي قبل مراجعة الحاجة الفعلية إليه.

## إجراءات تبدو مرشحة للمراجعة لاحقًا، لا للحذف الآن

بسبب طلب القراءة فقط، لم أحذف أو أغيّر أي ملف. بعد تثبيت قرار قاعدة الزاحف الصحيحة، يمكن مراجعة ما يلي:

1. توحيد عنوان قاعدة الزاحف بين الموصل وملفات GitHub Actions.
2. إصلاح أو توثيق آلية بناء `better-sqlite3` في CI، أو اعتماد بيئة Node/حزمة متوافقة بوضوح.
3. اختبار fallback الهجين فعليًا مع مواقع تفشل في Chromium.
4. التأكد من تطبيق حدود `max_pages` و`batch_size` وعدم تكرار الأهداف عند تشغيل أكثر من pass.
5. مراجعة ما إذا كان `crawl-site-queue` مسارًا مطلوبًا أم مسارًا موازيًا قديمًا، بناءً على الاستخدام الفعلي لا على أسماء الملفات.
6. إبقاء `reset-databases.yml` وأي مزامنة إنتاجية خارج التشغيل العادي مع حماية واضحة.
7. إعادة اختبار قاعدة Alzahf بعد توفير رمز صالح أو تصحيح بيانات الموصل؛ نتيجة `401` لا تسمح بتقييمها.

## الملفات التي لم تُغيّر

حالة Git قبل الفحص كانت نظيفة، ولم تُعدّل ملفات المستودع. الملف الوحيد المضاف هو هذا التقرير التوثيقي الخاص بالفحص.

## مرجع التحقق من Turso

استخدم الفحص صيغة Turso الرسمية: استبدال بروتوكول `libsql://` بـ`https://`، إرسال `Authorization: Bearer TOKEN`، استخدام `GET /health`، وتنفيذ SQL عبر `POST /v2/pipeline` وفق [مرجع SQL over HTTP الرسمي](https://docs.turso.tech/sdk/http/reference).
