const configs=[['production',process.env.TURSO_PRODUCTION_DATABASE_URL,process.env.TURSO_PRODUCTION_AUTH_TOKEN],['staging',process.env.TURSO_CRAWLER_DATABASE_URL,process.env.TURSO_CRAWLER_AUTH_TOKEN]];
const scalar=x=>x?.value??null;const endpoint=x=>x.replace(/^libsql:\/\//,'https://').replace(/^turso:\/\//,'https://').replace(/\/$/,'')+'/v2/pipeline';
async function pipe(url,token,requests){const r=await fetch(endpoint(url),{method:'POST',headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({requests:[...requests,{type:'close'}]})});const b=await r.json();if(!r.ok)throw Error(`${r.status} ${JSON.stringify(b)}`);return b.results||[];}
for(const [name,url,token] of configs){const schema=await pipe(url,token,[{type:'execute',stmt:{sql:"SELECT type,name,sql FROM sqlite_master WHERE sql IS NOT NULL ORDER BY type,name"}}]);const s=schema[0]?.response?.result;const objects=(s?.rows||[]).map(row=>Object.fromEntries(s.cols.map((c,i)=>[c.name,scalar(row[i])] )));const tables=objects.filter(x=>x.type==='table'&&!String(x.name).startsWith('sqlite_')).map(x=>x.name);const counts=[];for(const table of tables){try{const r=await pipe(url,token,[{type:'execute',stmt:{sql:`SELECT COUNT(*) AS n FROM "${table.replaceAll('"','""')}"`}}]);counts.push({table,rows:Number(scalar(r[0]?.response?.result?.rows?.[0]?.[0]))});}catch(error){counts.push({table,error:String(error)})}}console.log(JSON.stringify({name,tables,counts},null,2));}
