const stagingUrl=process.env.TURSO_CRAWLER_DATABASE_URL,stagingToken=process.env.TURSO_CRAWLER_AUTH_TOKEN,productionUrl=process.env.TURSO_PRODUCTION_DATABASE_URL,productionToken=process.env.TURSO_PRODUCTION_AUTH_TOKEN;
if(!stagingUrl||!stagingToken||!productionUrl||!productionToken)throw new Error('All staging and production Turso credentials are required');
const endpoint=x=>x.replace(/^libsql:\/\//,'https://').replace(/^turso:\/\//,'https://').replace(/\/$/,'')+'/v2/pipeline';const scalar=x=>x?.value??null;
async function pipeline(url,token,requests){const r=await fetch(endpoint(url),{method:'POST',headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({requests:[...requests,{type:'close'}]})});const b=await r.json();if(!r.ok)throw Error(`HTTP ${r.status}`);const e=(b.results||[]).filter(x=>x.type==='error'||x.response?.error);if(e.length)throw Error(`SQL error: ${JSON.stringify(e)}`);return b.results||[];}
const source=await pipeline(stagingUrl,stagingToken,[{type:'execute',stmt:{sql:"SELECT r.id,r.requested_url,r.canonical_url,r.title,r.description,r.extracted_text,r.search_text,r.icon_url,r.content_hash,r.http_status,v.validation_status FROM crawl_results r JOIN crawl_review_items v ON v.result_id=r.id WHERE r.distribution_status='review_queue' AND v.validation_status='approved' ORDER BY r.id"}}]);
const q=source[0].response.result;const rows=q.rows.map(a=>Object.fromEntries(q.cols.map((c,i)=>[c.name,scalar(a[i])] )));if(!rows.length)throw Error('No approved staging rows available');
const arg=(type,value)=>({type,value:String(value??'')});const rootUrl=new URL(rows[0].canonical_url).origin+'/';const rootName=rows.find(x=>x.canonical_url===rootUrl)?.title||'EgyptSchools.info';
const requests=[{type:'execute',stmt:{sql:"INSERT OR IGNORE INTO sites (name,url,canonical_url,description,icon_url,keywords,categories,priority,search_text,status,last_verified_at,failed_attempts,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,'active',CURRENT_TIMESTAMP,0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)",args:[arg('text',rootName),arg('text',rootUrl),arg('text',rootUrl),arg('text',''),arg('text',''),arg('text',''),arg('text',''),arg('integer',70),arg('text','')]}}];
for(const row of rows){const id=row.canonical_url||row.requested_url,name=row.title||id,description=row.description||'',content=row.extracted_text||'',searchText=row.search_text||'',icon=row.icon_url||'',hash=row.content_hash||'';requests.push({type:'execute',stmt:{sql:"INSERT OR IGNORE INTO site_pages (site_id,url,canonical_url,title,description,content,keywords,categories,icon_url,search_text,priority,status,http_status,crawl_status,content_hash,last_crawled_at,created_at,updated_at) VALUES ((SELECT id FROM sites WHERE canonical_url=?),?,?,?,?,?,?,?,?,?,70,'active',?,'crawled',?,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)",args:[arg('text',rootUrl),arg('text',row.requested_url||id),arg('text',id),arg('text',name),arg('text',description),arg('text',content),arg('text',''),arg('text',''),arg('text',icon),arg('text',searchText),arg('integer',row.http_status||200),arg('text',hash)]}});requests.push({type:'execute',stmt:{sql:"UPDATE site_pages SET title=?,description=?,content=?,icon_url=?,search_text=?,status='active',http_status=?,crawl_status='crawled',content_hash=?,last_crawled_at=CURRENT_TIMESTAMP,updated_at=CURRENT_TIMESTAMP WHERE canonical_url=? OR url=?",args:[arg('text',name),arg('text',description),arg('text',content),arg('text',icon),arg('text',searchText),arg('integer',row.http_status||200),arg('text',hash),arg('text',id),arg('text',row.requested_url||id)]}});requests.push({type:'execute',stmt:{sql:"DELETE FROM site_search_fts WHERE record_type='page' AND record_id IN (SELECT CAST(id AS TEXT) FROM site_pages WHERE canonical_url=? OR url=?)",args:[arg('text',id),arg('text',row.requested_url||id)]}});requests.push({type:'execute',stmt:{sql:"INSERT INTO site_search_fts (record_type,record_id,site_id,priority,title,description,content,keywords,categories,search_text) SELECT 'page',CAST(id AS TEXT),site_id,priority,title,description,content,keywords,categories,search_text FROM site_pages WHERE canonical_url=?",args:[arg('text',id)]}})}
requests.push({type:'execute',stmt:{sql:"DELETE FROM site_search_fts WHERE record_type='site' AND site_id=(SELECT id FROM sites WHERE canonical_url=?)",args:[arg('text',rootUrl)]}});requests.push({type:'execute',stmt:{sql:"INSERT INTO site_search_fts (record_type,record_id,site_id,priority,title,description,content,keywords,categories,search_text) SELECT 'site',CAST(id AS TEXT),id,priority,name,description,'',keywords,categories,search_text FROM sites WHERE canonical_url=?",args:[arg('text',rootUrl)]}});requests.push({type:'execute',stmt:{sql:"SELECT COUNT(*) AS pages FROM site_pages WHERE site_id=(SELECT id FROM sites WHERE canonical_url=?) AND status='active'",args:[arg('text',rootUrl)]}});requests.push({type:'execute',stmt:{sql:"SELECT COUNT(*) AS fts FROM site_search_fts WHERE site_id=(SELECT id FROM sites WHERE canonical_url=?)",args:[arg('text',rootUrl)]}});
const out=await pipeline(productionUrl,productionToken,requests);const pages=out.at(-3)?.response?.result?.rows?.[0]?.[0]?.value??null,fts=out.at(-2)?.response?.result?.rows?.[0]?.[0]?.value??null;if(Number(pages)<rows.length||Number(fts)<rows.length+1)throw Error(`Bulk verification failed: pages=${pages}, fts=${fts}, approved=${rows.length}`);console.log(JSON.stringify({ok:true,approved_rows:rows.length,root_url:rootUrl,production_pages:pages,production_fts:fts,validation:'approved_only'},null,2));
