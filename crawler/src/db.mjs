import fs from 'node:fs';
import path from 'node:path';
import Database from 'better-sqlite3';

const root=path.resolve(new URL('../..',import.meta.url).pathname);const dbDir=path.join(root,'db');fs.mkdirSync(dbDir,{recursive:true});const inputPath=process.env.CRAWLER_INPUT_DB_PATH||path.join(dbDir,'links.sqlite');const resultsPath=process.env.CRAWLER_RESULTS_DB_PATH||path.join(dbDir,'results.sqlite');export const db=new Database(inputPath);export const resultsDb=new Database(resultsPath);db.pragma('foreign_keys = ON');resultsDb.pragma('foreign_keys = ON');
 export function initDb(){db.exec(fs.readFileSync(path.join(root,'migrations/001_crawler_staging.sql'),'utf8'));for(const migration of ['007_discovery_monitoring.sql','008_discovery_queue.sql','009_job_locks.sql','010_discovery_sitemap_cursor.sql'])for(const statement of fs.readFileSync(path.join(root,'migrations',migration),'utf8').split(';').map(x=>x.trim()).filter(Boolean)){try{db.exec(statement)}catch(error){if(!/duplicate column name|already exists/i.test(String(error)))throw error;}}if(process.env.CRAWLER_SEED_STAGING==='1')db.exec(fs.readFileSync(path.join(root,'db/seed-staging.sql'),'utf8'));resultsDb.exec(fs.readFileSync(path.join(root,'migrations/002_crawler_results.sql'),'utf8'));for(const migration of ['003_distribution.sql','004_validation.sql','005_classification.sql'])for(const statement of fs.readFileSync(path.join(root,'migrations',migration),'utf8').split(';').map(x=>x.trim()).filter(Boolean)){try{resultsDb.exec(statement)}catch(error){if(!/duplicate column name|already exists/i.test(String(error)))throw error;}}}
export function canonicalize(raw){try{const u=new URL(raw);u.hash='';u.username='';u.password='';u.hostname=u.hostname.toLowerCase().replace(/^www\./,'');if((u.protocol==='https:'&&u.port==='443')||(u.protocol==='http:'&&u.port==='80'))u.port='';u.pathname=u.pathname.replace(/\/index\.(html?|php)$/i,'/')||'/';if(u.pathname.length>1)u.pathname=u.pathname.replace(/\/+$/,'');[...u.searchParams.keys()].filter(k=>/^(utm_|fbclid|gclid)/i.test(k)).forEach(k=>u.searchParams.delete(k));return u.toString();}catch{return '';}}
