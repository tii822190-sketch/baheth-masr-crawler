const CORS={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'GET,POST,OPTIONS','Access-Control-Allow-Headers':'Content-Type,Authorization'};
const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{...CORS,'content-type':'application/json;charset=utf-8'}});
export default {async fetch(request,env){if(request.method==='OPTIONS')return new Response(null,{headers:CORS});const u=new URL(request.url);if(u.pathname==='/health')return json({ok:true,service:'baheth-masr-crawler-control'});if(u.pathname==='/run'&&request.method==='POST'){if(!env.CRAWLER_CONTROL_TOKEN||request.headers.get('authorization')!==`Bearer ${env.CRAWLER_CONTROL_TOKEN}`)return json({error:'unauthorized'},401);const body=await request.json().catch(()=>({}));const type=['daily_check','weekly_refresh','manual'].includes(body.run_type)?body.run_type:'manual';const limit=Math.min(Number(body.limit)||10,100);const manualUrl=typeof body.manual_url==='string'?body.manual_url.slice(0,500):'';const ddgQueries=typeof body.ddg_queries==='string'?body.ddg_queries.slice(0,500):'';const r=await fetch(`https://api.github.com/repos/${env.GITHUB_REPO}/actions/workflows/crawler.yml/dispatches`,{method:'POST',headers:{accept:'application/vnd.github+json',authorization:`Bearer ${env.GITHUB_TOKEN}`,'user-agent':'baheth-masr-crawler-control','content-type':'application/json'},body:JSON.stringify({ref:env.GITHUB_REF||'main',inputs:{run_type:type,limit:String(limit),manual_url:manualUrl,ddg_queries:ddgQueries}})});return r.ok?json({ok:true,queued:true,run_type:type,limit,manual_url:Boolean(manualUrl),ddg_queries:ddgQueries?ddgQueries.split('|').slice(0,3):[]}):json({ok:false,error:await r.text()},502)}return json({service:'baheth-masr-crawler-control',endpoints:['GET /health','POST /run']});}};
